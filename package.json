{
  "name": "quick-ship-label-preview",
  "version": "2.5.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check background.js && node --check content.js && node --check injected.js && node --check popup.js && node --check resolver.js && node --check utils.js && node --check viewer-utils.js && node --check viewer.js"
  }
}
